# Production Environment Variables
# ⚠️ IMPORTANTE: NÃO commitar este arquivo com valores reais!

# Application
NODE_ENV=production
PORT=3000
API_PREFIX=api/v1

# Database
# Use conexão PostgreSQL de produção (Railway, Render, AWS RDS, etc)
DATABASE_URL=postgresql://user:password@host:port/database?schema=public

# JWT
# ⚠️ CRÍTICO: Gerar chave forte e única para produção
# Sugestão: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_KEY_IN_PRODUCTION
JWT_EXPIRATION=7d

# CORS
# Domínio do frontend em produção (Vercel, Netlify, etc)
CORS_ORIGIN=https://seudominio.com.br

# Logs
LOG_LEVEL=info

# Storage Configuration
# Options: 'local' or 's3'
# Use 's3' for production to leverage cloud storage benefits
STORAGE_TYPE=s3

# AWS S3 Configuration (required if STORAGE_TYPE=s3)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1
AWS_BUCKET=portal-locadora-uploads

# Email (opcional - para notificações)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASS=sua-senha-app
# SMTP_FROM=Portal da Locadora <noreply@portaldalocadora.com.br>

# Monitoring (opcional)
# SENTRY_DSN=https://your-sentry-dsn
